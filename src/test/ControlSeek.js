import {describe, it} from 'mocha';
import expect from 'expect';

describe('When moving sliderbar', () => {
  it('should fire an event of move with position', () => {
    expect(true).toBe(true);
  });
});
