import Features from '../Features';

export default class ControlVolume {
  constructor (events) {
    this.events = events;
  }

  init () {

  }

  destroy () {

  }
};

Features.create('ControlVolume', ControlVolume);
